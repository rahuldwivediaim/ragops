# =============================================================================
# Development Configuration
# Overrides base.yaml
# =============================================================================

application:
  environment: "development"

logging:
  level: "DEBUG"

providers:
  llm:
    timeout: 60

api:
  host: "127.0.0.1"
  port: 8000
